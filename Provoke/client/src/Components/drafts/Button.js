//maybe I don't need a button handler, instead need parallel normal vs tutorial
// export const Button = ( {label, className} ) => {
//     //if user.normalMode===true, onclick should edit and delete normally
//     // else onclick should do weird pranks. 
//     return (
//             <button className={className} >{label}</button>
//     );
// };