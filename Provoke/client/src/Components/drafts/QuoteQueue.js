import { useEffect, useState } from "react";
import { getAllPlaceholders } from "../../Managers/PlaceholderManager"


export const QuoteQueue = () => {
    //set state for the placeholders list in this 
    // const [placeholders, setPlaceholders] = useState([]);

    // useEffect(() => {
    //     getAllPlaceholders()
    //     .then((placeholders) => setPlaceholders(placeholders))
    // })


    // let finalQuote = {}

  
    // let quoteList = placeholders.filter(placeholder => placeholder.id !== 8)

    // let randomQuote

    // randomQuote = quoteList[Math.floor(Math.random() * quoteList.length)]

    // let quote = randomQuote

    // finalQuote = quote
    
    // return finalQuote

}